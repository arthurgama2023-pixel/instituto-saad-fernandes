// Sala de teleconsulta: tela cheia, fundo escuro, sem bottom nav.
export default function SalaLayout({ children }: { children: React.ReactNode }) {
  return <div className="brand-app">{children}</div>;
}
